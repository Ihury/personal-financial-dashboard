import React from "react";
import ReactDOM from "react-dom/client";
import FinancialDashboard from "./App";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <FinancialDashboard />
  </React.StrictMode>
);
